[project]
name = "nyc-taxi-etl"
version = "1.0.0"
description = "A Python/Airflow/Polars project"
requires-python = ">=3.12"
dependencies = [
    "polars==1.44.0"
]

[project.optional-dependencies]
test = [
    "pytest",
]